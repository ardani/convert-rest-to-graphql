# import Thumbnail from "base.graphql"
# import ComicResults from "comic.graphql"

type Hero {
    id: ID!
    name: String
    description: String,
    thumbnail: Thumbnail,
    comics(offset: Int, limit:Int): ComicResults
}